import React from 'react'

const Main = () => {
    return (
        <div>
            <main>
                <h3><strong>Build</strong>a login/auth app with MERN STACK</h3>
                <h5>Create a minimal full-stack app with user authentication via JWT token</h5>
                <button>SIGN UP</button>
                <button>LOG IN</button>
            </main>
        </div>
    )
};

export default Main;
